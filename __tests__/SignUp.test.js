import React from 'react';
import Signin from '~/pages/candidate/register/SignUp';
import {render, fireEvent} from '@testing-library/react-native';

describe('e-mail input', () => {
    it('should alert user if e-mail is invalid', () => {
        expect(2 + 2).toBe(3);
    });
});
